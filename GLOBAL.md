# ------ A. 语言与规则目录 ------

- 所有与用户的沟通内容**以及由 Agent 输出的说明、方案、分析、文档**，均需使用中文。
- 技术专有名词、代码标识、缩写可保留英文（如 API、Python、DTO、TypeScript 等）。
- 项目级规则文件统一放置在 `.agent/rules` 目录中，所有行为需遵循该目录下的规范。

# ------ 1. 基本原则 ------

- 在编写任何代码前，先理解项目结构、依赖、上下文。
- 如遇到不确定的情况，优先询问用户，而不是自行补全。
- 保持生成内容准确可执行，不瞎编 API、类型、字段、接口路径。

# ------ 2. 代码风格 ------

- 永远遵循项目现有风格（格式化、命名、lint 规则）。
- Typescript 优先，能类型化就不要写 any。
- 遵循 ESLint + Prettier 规则，不生成与规则冲突的代码。
- 避免不必要的抽象，保持简洁、可维护。

# ------ 3. 项目改动规则 ------

- 修改文件时，只修改必要部分，不重写整文件。
- 如需大范围重构，必须先解释理由并征求确认。
- 更新内容时保证向后兼容，不破坏现有功能。

# ------ 4. 解释与说明 ------

- 默认不输出多余解释，除非用户要求 Explain。
- 若需解释：简短、准确、基于当前代码，不编造不存在的逻辑。

# ------ 5. 依赖与库 ------

- 不推荐未在项目中出现的依赖，如需添加必须提醒用户。
- 保证生成的代码兼容当前依赖版本，不使用不存在的 API。

# ------ 6. 文件定位 ------

- 对于多模块或 monorepo，先推断相关文件位置再修改。
- 如不确定文件路径，询问用户或提供候选列表。

# ------ 7. 模型行为限制 ------

- 禁止生成虚构内容（接口、字段、配置、数据库 schema 等）。
- 不要自动重写整文件，除非严重错误或用户明确要求。
- 涉及风险性操作（删除、重构）需先征求用户确认。

# ------ 8. 日志与调试 ------

- 默认不加入 console.log。
- 如用户要求调试，则加入可控、可关闭的 debug 代码。

# ------ 9. UI 与前端规则 ------

- 组件保持职责单一、结构清晰。
- CSS/样式遵循项目现有规范（Tailwind / CSS Modules / SCSS 等）。
- 不随意创建新的 UI 组件目录结构。

# ------ 10. 安全性 ------

- 不输出敏感信息（token、密码等）。
- 后端逻辑使用安全实践。
